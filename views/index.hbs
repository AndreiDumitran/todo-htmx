<div class="max-w-lg mx-auto text-white">
  <h1 class="text-5xl font-bold my-12 text-center mb-2 font-alata">Todo App!</h1>
  {{>todoInput}}
  {{>filterBtns}}
  <div class="flex flex-col mt-12 text-2xl">
    {{#each todos}}
      {{>todoItem}}
    {{/each}}
  </div>
</div>
